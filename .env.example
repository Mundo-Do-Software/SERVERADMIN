# Exemplo de arquivo de ambiente para produção
# Copie este arquivo para .env e modifique os valores

# Chave secreta para JWT (OBRIGATÓRIO - gere uma chave segura)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Configurações do banco PostgreSQL
POSTGRES_DB=server_admin
POSTGRES_USER=admin
POSTGRES_PASSWORD=your-secure-postgres-password

# Configurações do Redis
REDIS_PASSWORD=your-secure-redis-password

# URLs CORS permitidas (separadas por vírgula)
BACKEND_CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# URL do banco (para produção com PostgreSQL)
DATABASE_URL=postgresql://admin:your-secure-postgres-password@postgres:5432/server_admin

# URL do Redis
REDIS_URL=redis://:your-secure-redis-password@redis:6379

# Configurações de log
LOG_LEVEL=INFO

# Configurações de email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Configurações de SSL
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Configurações de backup
BACKUP_RETENTION_DAYS=30
