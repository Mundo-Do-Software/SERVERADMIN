# Default configuration settings for the server admin installer

# Database configuration
DB_USER="serveradmin"
DB_NAME="serveradmin"
DB_PASSWORD=""

# NGINX configuration
NGINX_SITE="serveradmin"
NGINX_PORT=80

# SSL configuration
SSL_ENABLED=false
SSL_EMAIL="admin@example.com"

# Redis configuration
REDIS_ENABLED=true

# Other settings
AUTO_INSTALL=false
SKIP_SSL=false